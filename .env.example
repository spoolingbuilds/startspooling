# =============================================================================
# ENVIRONMENT CONFIGURATION FOR STARTSPOOLING EMAIL VERIFICATION FLOW
# =============================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual configuration
# 3. NEVER commit the .env file to version control
#
# RESEND API KEY SETUP:
# 1. Sign up at https://resend.com
# 2. Create a new API key in your dashboard
# 3. Replace "re_xxxxxxxxxxxxx" with your actual API key
# 4. Ensure your domain is verified in Resend dashboard
#
# DATABASE SETUP:
# 1. Install PostgreSQL locally or use a cloud provider
# 2. Create a database named "startspooling"
# 3. Update the connection string with your credentials
# 4. Run: npm run db:setup (or equivalent setup script)
#
# SECURITY NOTES:
# - Never commit .env files to version control
# - Use strong, unique passwords for database
# - Rotate API keys regularly
# - Use environment-specific configurations for production
# =============================================================================

# Database Configuration
# PostgreSQL connection string for the main application database
DATABASE_URL="postgresql://user:password@localhost:5432/startspooling"

# Email Service Configuration (Resend)
# Your Resend API key for sending verification emails
RESEND_API_KEY="re_xxxxxxxxxxxxx"

# Site Configuration
# Public URL of your application (used for email links)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
# Public site name (used in emails and UI)
NEXT_PUBLIC_SITE_NAME="StartSpooling"

# Verification Settings
# Length of verification codes (6 digits recommended)
VERIFICATION_CODE_LENGTH=6
# How long verification codes remain valid (in minutes)
VERIFICATION_CODE_EXPIRY_MINUTES=15
# Maximum verification attempts before lockout
MAX_VERIFICATION_ATTEMPTS=4
# Duration of account lockout after max attempts (in hours)
LOCKOUT_DURATION_HOURS=1

# Rate Limiting Configuration
# Maximum signup attempts per IP address per hour
RATE_LIMIT_SIGNUP_PER_IP_HOUR=3
# Maximum verification attempts per IP address per hour
RATE_LIMIT_VERIFY_PER_IP_HOUR=10
# Maximum resend attempts per email address per hour
RATE_LIMIT_RESEND_PER_EMAIL_HOUR=3
# Maximum emails sent per hour (global limit)
RATE_LIMIT_EMAIL_SEND_PER_HOUR=5

# Environment Configuration
# Current environment (development, staging, production)
NODE_ENV="development"
